
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Lovely R. Montiagodo — Portfolio</title>
<meta name="description" content="Lovely R. Montiagodo — BSIT 2A student, singer, and dancer.">

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,380;9..144,500;9..144,600;9..144,700&family=Work+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">

<style>
  :root {
    --bg: #0e0c0a;
    --bg-soft: #17140f;
    --card: #1b1712;
    --card-hover: #221c15;
    --gold: #cda05a;
    --gold-soft: #e8c98a;
    --gold-deep: #a9822f;
    --cream: #f3ede0;
    --ink-soft: #b9ae9b;
    --ink-dim: #776d5c;
    --line: rgba(243, 237, 224, 0.10);

    --font-display: "Fraunces", Georgia, serif;
    --font-body: "Work Sans", -apple-system, sans-serif;
    --font-mono: "IBM Plex Mono", ui-monospace, monospace;
  }

  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; }
  @media (prefers-reduced-motion: reduce) {
    html { scroll-behavior: auto; }
    * { animation-duration: 0.001ms !important; transition-duration: 0.001ms !important; }
  }

  body {
    margin: 0;
    background: var(--bg);
    color: var(--cream);
    font-family: var(--font-body);
    line-height: 1.55;
  }

  a { color: var(--gold); }
  h1, h2, h3 { font-family: var(--font-display); font-weight: 600; line-height: 1.05; margin: 0; color: var(--cream); }
  p { margin: 0; }

  .skip-link {
    position: absolute; left: -999px; top: 0;
    background: var(--gold); color: var(--bg);
    padding: 0.75rem 1.25rem; z-index: 200;
    font-family: var(--font-mono); font-size: 0.85rem;
  }
  .skip-link:focus { left: 1rem; top: 1rem; }
  a:focus-visible, button:focus-visible { outline: 2px solid var(--gold); outline-offset: 3px; }

  .wrap { max-width: 1120px; margin: 0 auto; padding: 0 2rem; }
  @media (max-width: 640px) { .wrap { padding: 0 1.25rem; } }

  .eyebrow {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--gold);
    display: block;
    margin-bottom: 0.9rem;
  }

  section { padding: 5.5rem 0; border-bottom: 1px solid var(--line); }
  @media (max-width: 640px) { section { padding: 3.5rem 0; } }

  .btn {
    display: inline-flex; align-items: center; gap: 0.5rem;
    font-family: var(--font-mono); font-size: 0.78rem;
    padding: 0.9rem 1.6rem; border-radius: 999px;
    text-decoration: none; border: 1px solid var(--gold-deep);
    color: var(--cream); background: transparent;
    transition: background 0.15s ease, color 0.15s ease, transform 0.15s ease, border-color 0.15s ease;
  }
  .btn:hover { transform: translateY(-2px); background: var(--gold); border-color: var(--gold); color: var(--bg); }
  .btn-solid { background: var(--gold); border-color: var(--gold); color: var(--bg); }
  .btn-solid:hover { background: var(--gold-soft); border-color: var(--gold-soft); color: var(--bg); }

  /* ============ NAV ============ */
  header.nav {
    position: sticky; top: 0; z-index: 100;
    background: rgba(0, 0, 0, 0.973);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--line);
  }
  .nav-inner { display: flex; align-items: center; justify-content: flex-end; padding: 1.1rem 0; }
  .nav-links { display: flex; gap: 1.8rem; list-style: none; margin: 0; padding: 0; }
  .nav-links a { font-family: var(--font-mono); font-size: 0.78rem; letter-spacing: 0.04em; color: var(--ink-soft); text-decoration: none; }
  .nav-links a:hover { color: var(--gold); }
  @media (max-width: 640px) { .nav-links { gap: 1.1rem; } .nav-links a { display: none; } .nav-links a:first-child, .nav-links a:last-child { display: inline; } }

  /* ============ HERO ============ */
  .hero {
    padding-top: 25rem;
    padding-bottom: 1rem;
    border-bottom: none;
    position: relative;
    overflow: hidden;
    background:
      radial-gradient(60% 55% at 78% 32%, rgba(205, 160, 90, 0.16) 0%, rgba(205, 160, 90, 0) 70%),
      linear-gradient(100deg, rgba(14, 12, 10, 0.068) 32%, rgba(14,12,10,0.55) 100%),
      url("images/lovely.jpg");
    background-size: cover;
    background-repeat: no-repeat;
    min-height: 82vh;
    display: flex;
    align-items: center;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 2.5rem;
    align-items: center;
    width: 100%;
  }
  @media (max-width: 860px) { .hero-grid { grid-template-columns: 1fr; } }

  .hero-content {
    margin-top: -12rem;
  }

  .hero h1 {
    font-size: clamp(2.6rem, 6.4vw, 4.6rem);
    letter-spacing: -0.01em;
    font-weight: 600;
    color: var(--cream);
  }
  .hero h1 .accent-name { color: var(--gold); }

  .hero-tagline { font-family: var(--font-display); font-style: italic; font-size: clamp(1.1rem, 2vw, 1.4rem); font-weight: 500; margin-top: 1.2rem; max-width: 34ch; color: var(--ink-soft); }
  .hero-tagline em { font-style: italic; color: var(--gold); }

  .hero-desc {
    font-family: var(--font-body);
    font-size: 0.98rem;
    color: var(--ink-soft);
    max-width: 44ch;
    margin-top: 1.2rem;
  }

  .hero-actions { display: flex; gap: 0.8rem; flex-wrap: wrap; margin-top: 2rem; }

  .hero-social { display: flex; gap: 1rem; margin-top: 2.2rem; }
  .hero-social a {
    width: 36px; height: 36px; border-radius: 50%; border: 1px solid var(--line);
    display: flex; align-items: center; justify-content: center; color: var(--ink-soft);
    text-decoration: none; transition: border-color 0.15s ease, color 0.15s ease;
  }
  .hero-social a:hover { border-color: var(--gold); color: var(--gold); }

  /* ============ SECTION HEADS ============ */
  .section-head { display: flex; align-items: baseline; justify-content: space-between; margin-bottom: 2.5rem; gap: 1rem; flex-wrap: wrap; }
  .section-head h2 { font-size: clamp(1.6rem, 3vw, 2.1rem); letter-spacing: 0.01em; }

  /* ============ ABOUT ============ */
  #about .section-head { justify-content: center; text-align: center; }
  #about .section-head > div { width: 100%; }

  .about-layout { display: flex; justify-content: center; }
  .about-copy { max-width: 640px; margin: 0 auto; text-align: center; }

  .about-copy p.lead { font-family: var(--font-display); font-size: 1.35rem; font-weight: 500; max-width: 34ch; color: var(--gold-soft); margin-left: auto; margin-right: auto; text-align: center; }
  .about-copy p.body { color: var(--ink-soft); font-size: 0.98rem; max-width: 52ch; margin-top: 1.2rem; margin-left: auto; margin-right: auto; text-align: center; }

  .facts-panel { background: var(--card); border: 1px solid var(--line); border-radius: 12px; padding: 0.4rem; }
  .fact-row { padding: 1.4rem 1.5rem; display: flex; align-items: center; gap: 1rem; }
  .fact-row + .fact-row { border-top: 1px solid var(--line); }
  .fact-icon { width: 36px; height: 36px; flex-shrink: 0; color: var(--gold); }
  .fact-row .num { font-family: var(--font-display); font-size: 1.4rem; font-weight: 600; }
  .fact-row .label { font-family: var(--font-mono); font-size: 0.68rem; color: var(--ink-soft); text-transform: uppercase; letter-spacing: 0.05em; }

  /* ============ SKILLS ============ */
  .skills-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.2rem; }
  @media (max-width: 900px) { .skills-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 480px) { .skills-grid { grid-template-columns: 1fr; } }
  .skill-card {
    background: var(--card); border: 1px solid var(--line); border-radius: 12px;
    padding: 1.8rem; transition: transform 0.2s ease, border-color 0.2s ease, background 0.2s ease;
  }
  .skill-card:hover { transform: translateY(-4px); border-color: var(--gold-deep); background: var(--card-hover); }
  .skill-icon {
    width: 42px; height: 42px; border-radius: 10px; background: rgba(205,160,90,0.10);
    display: flex; align-items: center; justify-content: center; color: var(--gold);
    margin-bottom: 1.1rem;
  }
  .skill-card h3 { font-size: 1.02rem; margin-bottom: 0.5rem; color: var(--cream); }
  .skill-card p { color: var(--ink-soft); font-size: 0.86rem; }

  /* ============ WORKS ============ */
  .work-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.4rem; }
  @media (max-width: 900px) { .work-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 480px) { .work-grid { grid-template-columns: 1fr; } }
  .work-card { text-decoration: none; color: inherit; display: block; }
  button.work-card {
    font: inherit;
    text-align: left;
    border: none;
    background: none;
    padding: 0;
    cursor: pointer;
    width: 100%;
  }
  .work-media { aspect-ratio: 4/5; border-radius: 10px; overflow: hidden; background: var(--card); border: 1px solid var(--line); position: relative; transition: border-color 0.2s ease; }
  .work-card:hover .work-media { border-color: var(--gold-deep); }
  .work-media img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .work-media .placeholder { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; color: var(--ink-dim); font-family: var(--font-mono); font-size: 0.7rem; text-align: center; padding: 1rem; }
  .work-media .arrow {
    position: absolute; top: 0.7rem; right: 0.7rem; width: 30px; height: 30px; border-radius: 50%;
    background: rgba(14,12,10,0.6); border: 1px solid var(--line); display: flex; align-items: center; justify-content: center;
    color: var(--gold); opacity: 0; transform: translate(-4px, 4px); transition: opacity 0.2s ease, transform 0.2s ease;
  }
  .work-card:hover .arrow { opacity: 1; transform: translate(0,0); }
  .work-card h3 { font-size: 1rem; margin-top: 0.9rem; color: var(--cream); }
  .work-card .meta { font-family: var(--font-mono); font-size: 0.72rem; color: var(--ink-soft); margin-top: 0.3rem; }

  /* ============ EXPERIENCE — TIMELINE GRID ============ */
  .timeline {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2.4rem 3rem;
    max-width: 900px;
  }
  @media (max-width: 640px) {
    .timeline { grid-template-columns: 1fr; }
  }
  .timeline-item {
    position: relative;
    padding-top: 1.4rem;
  }
  .timeline-dot {
    position: absolute;
    left: 0;
    top: 0;
    width: 11px;
    height: 11px;
    border-radius: 50%;
    background: var(--gold);
    box-shadow: 0 0 0 4px rgba(205, 160, 90, 0.14);
  }
  .timeline-item::before {
    content: "";
    position: absolute;
    left: 5px;
    top: 11px;
    width: 1px;
    height: 0.9rem;
    background: var(--line);
  }
  .timeline-date {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.04em;
    color: var(--ink-dim);
    text-transform: uppercase;
    display: block;
    margin-bottom: 0.4rem;
  }
  .timeline-item h3 {
    font-size: 1.1rem;
    color: var(--cream);
    margin-bottom: 0.2rem;
  }
  .timeline-org {
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--gold-soft);
    display: block;
    margin-bottom: 0.6rem;
  }
  .timeline-item p.desc {
    color: var(--ink-soft);
    font-size: 0.9rem;
    max-width: 42ch;
  }

  /* ============ CONTACT CTA ============ */
  .cta-banner {
    border-radius: 0;
    background: none;
    border: none;
    color: var(--cream);
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1.6rem;
  }
  .cta-banner h2 { color: var(--cream); font-size: clamp(1.5rem, 3vw, 2rem); max-width: 22ch; }
  .cta-banner .sub { color: var(--ink-soft); margin-top: 0.7rem; max-width: 38ch; font-size: 0.92rem; }
  .cta-actions { display: flex; gap: 0.9rem; flex-wrap: wrap; }
  .contact { border-bottom: none; }

  footer.sign-off {
    padding: 2rem 0 3rem; font-family: var(--font-mono); font-size: 0.74rem; color: var(--ink-soft);
    display: flex; justify-content: space-between; flex-wrap: wrap; gap: 1rem;
  }
  footer.sign-off a { color: var(--ink-soft); text-decoration: none; }
  footer.sign-off a:hover { color: var(--gold); }
  .footer-links { display: flex; gap: 1.3rem; }

  .reveal { opacity: 0; transform: translateY(14px); transition: opacity 0.6s ease, transform 0.6s ease; }
  .reveal.in { opacity: 1; transform: translateY(0); }

  /* ============ LIGHTBOX MODAL ============ */
  .lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 1000;
    background: rgba(14, 12, 10, 0.92);
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }
  .lightbox.open { display: flex; }
  .lightbox-img {
    max-width: 90vw;
    max-height: 85vh;
    border-radius: 10px;
    border: 1px solid var(--line);
    box-shadow: 0 20px 60px rgba(0,0,0,0.5);
  }
  .lightbox-caption {
    position: absolute;
    bottom: 1.6rem;
    left: 0; right: 0;
    text-align: center;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--ink-soft);
  }
  .lightbox-close {
    position: absolute;
    top: 1.4rem; right: 1.6rem;
    width: 40px; height: 40px;
    border-radius: 50%;
    border: 1px solid var(--line);
    background: rgba(255,255,255,0.04);
    color: var(--cream);
    font-size: 1.3rem;
    line-height: 1;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    transition: border-color 0.15s ease, color 0.15s ease;
  }
  .lightbox-close:hover { border-color: var(--gold); color: var(--gold); }


.contact {
  border-bottom: none;
  position: relative;
  background:
    linear-gradient(180deg, rgba(14, 12, 10, 0) 0%, rgba(14, 12, 10, 0.644) 100%),
    url("images/43b2fcc9cb84c925a1b015f5e71b5b61.jpg");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

#skills {
  position: relative;
  background:
    linear-gradient(180deg, rgba(14,12,10,0.90) 0%, rgba(14, 12, 10, 0.432) 100%),
    url("images/6b23e6574fce56106c82a7812cfc1a58.jpg");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

#process {
  position: relative;
  background:
    linear-gradient(180deg, rgba(14,12,10,0.90) 0%, rgba(14, 12, 10, 0.671) 100%),
    url("images/c240085e286a24f907471d343d21d252.jpg");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

#projects {
  position: relative;
  background:
    linear-gradient(180deg, rgba(14,12,10,0.90) 0%, rgba(14, 12, 10, 0.747) 100%),
    url("images/c240085e286a24f907471d343d21d252.jpg");
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
}

</style>
</head>
<body>

<a class="skip-link" href="#main">Skip to content</a>

<header class="nav">
  <div class="wrap nav-inner">
    <nav aria-label="Section navigation">
      <ul class="nav-links">
        <li><a href="#about">About</a></li>
        <li><a href="#skills">Skills</a></li>
        <li><a href="#projects">Projects</a></li>
        <li><a href="#process">Experience</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
    </nav>
  </div>
</header>

<main id="main">

<!-- ============ HERO ============ -->
<section class="hero" id="hero">
  <div class="wrap">
    <div class="hero-grid">
      <div class="hero-content">
        <h1>MONTIAGODO <span class="accent-name">LOVELY</span></h1>
        <span class="eyebrow">Bachelor of Science in Information Technology 2A &amp; Singer/Dancer</span>
        <p class="hero-tagline">I Study i Sing i Dance and that's what makes me happy.</p>
        <p class="hero-desc">A BSIT student from Eastern Samar State University who splits her time between writing code, singing, and choreographing routines, bringing the same discipline for structure and timing to all three.</p>
        <div class="hero-actions">
          <a class="btn" href="mailto:lovely.montiagodo@example.com">Email</a>
          <a class="btn" href="images/Lovely_Montiagodo_Resume.pdf" target="_blank" rel="noopener">Resume</a>
          <a class="btn" href="#" target="_blank" rel="noopener">GitHub</a>
          <a class="btn" href="#" target="_blank" rel="noopener">LinkedIn</a>
        </div>
        <div class="hero-social"></div>
      </div>
    </div>
  </div>
</section>

  <!-- ============ ABOUT ============ -->
  <section id="about">
    <div class="wrap reveal">
      <div class="section-head">
        <div>
          <h2>About me</h2>
        </div>
      </div>

      <div class="about-layout">
        <div class="about-copy">
          <p class="lead">Two disciplines, one habit: build with structure, move with intention.</p>
          <p class="body">Hi, I'm Lovely R. Montiagodo, a 2nd year BSIT student (Section 2A) at Eastern Samar State University. Between programming assignments, choir practice, and dance rehearsals, I've picked up the same habit three times over — clean structure, good timing, and no wasted movement.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ SKILLS ============ -->
  <section id="skills">
    <div class="wrap reveal">
      <div class="section-head">
        <div>
          <h2>Skills</h2>
        </div>
      </div>
      <div class="skills-grid">
        <div class="skill-card">
          <div class="skill-icon"><svg width="18" height="18" viewBox="0 0 18 18" fill="none"><rect x="2" y="2" width="14" height="14" rx="3" stroke="currentColor" stroke-width="1.4"/><path d="M6 9h6M9 6v6" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/></svg></div>
          <h3>Basics in Programming</h3>
          <p>Creating Portfolio and other Activities.</p>
        </div>
        

        <div class="skill-card">
          <div class="skill-icon"><svg width="18" height="18" viewBox="0 0 18 18" fill="none"><circle cx="9" cy="9" r="6.5" stroke="currentColor" stroke-width="1.4"/><path d="M9 5.5V9l2.5 1.5" stroke="currentColor" stroke-width="1.4" stroke-linecap="round"/></svg></div>
          <h3>Designing</h3>
          <p>Designing Systems like portfolio.</p>
        </div>
        <div class="skill-card">
          <div class="skill-icon"><svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M12 3c3 3 5 6 5 9a5 5 0 0 1-10 0c0-3 2-6 5-9Z" stroke="currentColor" stroke-width="1.3" stroke-linejoin="round"/></svg></div>
          <h3>Singing &amp; Dance</h3>
          <p>Folk and contemporary dance, plus vocal performance for school events and local showcases.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ PROJECTS ============ -->
  <section id="projects">
    <div class="wrap reveal">
      <div class="section-head">
        <div>
          <h2>PROJECTS</h2>
        </div>
      </div>
      <div class="work-grid">
        <button type="button" class="work-card" onclick="openLightbox('images/01a56feb9677a51b84f25a99762e9bdb.jpg', 'Graphics Design')">
          <div class="work-media">
            <img src="images/01a56feb9677a51b84f25a99762e9bdb.jpg" alt="Library Management System screenshot" loading="lazy">
            <span class="arrow" aria-hidden="true">
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 11L11 3M11 3H5M11 3V9" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </span>
          </div>
          <h3>Graphics Design</h3>
        </button>

        <button type="button" class="work-card" onclick="openLightbox('images/Screenshot 2026-08-16 091546.png', 'Portfolio')">
          <div class="work-media">
            <img src="images/Screenshot 2026-08-16 091546.png" alt="Student Records Database screenshot" loading="lazy">
            <span class="arrow" aria-hidden="true">
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M3 11L11 3M11 3H5M11 3V9" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </span>
          </div>
          <h3>My Portfolio</h3>
        </button>
      </div>
    </div>
  </section>

  <!-- ============ EXPERIENCE ============ -->
  <section id="process">
    <div class="wrap reveal">
      <div class="section-head">
        <div>
          <h2>EXPERIENCE</h2>
        </div>
      </div>
      <div class="timeline">
        <div class="timeline-item">
          <span class="timeline-dot"></span>
          <span class="timeline-date">2024 — 2025</span>
          <h3>Computer Fundamentals</h3>
          <span class="timeline-org">2nd-Year BSIT</span>
          <p class="desc">Built a foundation in computer basics, file systems, and office applications as part of 2nd-year BSIT.</p>
        </div>

        <div class="timeline-item">
          <span class="timeline-dot"></span>
          <span class="timeline-date">2023 — Present</span>
          <h3>Choir &amp; Dance Troupe</h3>
          <span class="timeline-org">School / Community</span>
          <p class="desc">Sang and performed traditional Filipino folk dances at school cultural events and local community showcases.</p>
        </div>

        <div class="timeline-item">
          <span class="timeline-dot"></span>
          <span class="timeline-date">2025 — 2025</span>
          <h3>HTML &amp; CSS Basics</h3>
          <span class="timeline-org">Coursework</span>
          <p class="desc">Learned front-end fundamentals — semantic HTML, layout with CSS, and simple responsive design.</p>
        </div>

        <div class="timeline-item">
          <span class="timeline-dot"></span>
          <span class="timeline-date">2025 — Present</span>
          <h3>Contemporary Dance &amp; Vocals</h3>
          <span class="timeline-org">Independent / Showcases</span>
          <p class="desc">Expanded into contemporary choreography and solo singing, joining rehearsals and small showcases outside school.</p>
        </div>

        <div class="timeline-item">
          <span class="timeline-dot"></span>
          <span class="timeline-date">2026 — Present</span>
          <h3>First Portfolio Site</h3>
          <span class="timeline-org">Personal Project</span>
          <p class="desc">Designed and coded this personal portfolio from scratch — planning, layout, and JavaScript basics included.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- ============ CONTACT ============ -->
  <section class="contact" id="contact">
    <div class="wrap reveal">

      <div class="section-head">
        <div>
          <h2>CONTACT</h2>
        </div>
      </div>

      <div class="cta-banner">
        <div>
          <h2>Let's create something together.</h2>
          <p class="sub">Open to group projects, OJT/internship opportunities, and the occasional singing or dance collab.</p>
        </div>
        <div class="cta-actions">
          <a class="btn btn-solid" href="mailto:lovely.montiagodo@example.com">Get in touch</a>
          <a class="btn" href="images/Lovely_Montiagodo_Resume.pdf" target="_blank" rel="noopener">Resume</a>
        </div>
      </div>
    </div>
  </section>

  <footer class="sign-off">
    <div class="wrap" style="display:flex; justify-content:space-between; flex-wrap:wrap; gap:1rem; width:100%;">
      <span>© 2026 Lovely R. Montiagodo. All rights reserved.</span>
      <div class="footer-links">
        <a href="mailto:lovely.montiagodo@example.com">Email</a>
        <a href="#">Facebook</a>
        <a href="#">Instagram</a>
      </div>
    </div>
  </footer>

</main>

<!-- ============ LIGHTBOX MODAL (page-level, outside main content flow) ============ -->
<div class="lightbox" id="lightbox" onclick="closeLightbox(event)">
  <button type="button" class="lightbox-close" onclick="closeLightbox(event)" aria-label="Close">&times;</button>
  <img class="lightbox-img" id="lightbox-img" src="" alt="">
  <div class="lightbox-caption" id="lightbox-caption"></div>
</div>

<script>
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('in');
        revealObserver.unobserve(entry.target);
      }
    });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

  function openLightbox(src, caption) {
    const lb = document.getElementById('lightbox');
    document.getElementById('lightbox-img').src = src;
    document.getElementById('lightbox-img').alt = caption;
    document.getElementById('lightbox-caption').textContent = caption;
    lb.classList.add('open');
    document.body.style.overflow = 'hidden';
  }
  function closeLightbox(e) {
    if (e.target.id === 'lightbox' || e.target.classList.contains('lightbox-close')) {
      document.getElementById('lightbox').classList.remove('open');
      document.body.style.overflow = '';
    }
  }
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      document.getElementById('lightbox').classList.remove('open');
      document.body.style.overflow = '';
    }
  });
</script>

</body>
</html>

